/**
 * Audio recorder — Phase 1 stub.
 * Full implementation (MediaRecorder + AudioContext pipeline) coming in Phase 2.
 */

let _mediaRecorder = null
let _stream = null

/**
 * Start recording from the user's microphone.
 * @returns {Promise<void>}
 */
export async function startRecording() {
  // Stub — no-op for now
  return
}

/**
 * Stop the current recording.
 * @returns {Promise<AudioBuffer|null>} The recorded audio buffer, or null.
 */
export async function stopRecording() {
  // Stub — no-op for now
  return null
}
