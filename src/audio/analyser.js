/**
 * Audio feature extractor — Phase 1 stub.
 * Full implementation (Meyda MFCC + spectral features) coming in Phase 2.
 */

/**
 * Extract audio features from an AudioBuffer using Meyda.
 * @param {AudioBuffer} audioBuffer
 * @returns {Promise<Float32Array|null>} Feature vector, or null on failure.
 */
export async function extractFeatures(audioBuffer) {
  // Stub — returns null for now
  return null
}
