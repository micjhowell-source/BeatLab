/**
 * Demo sound synthesiser — Phase 1 stub.
 * Full implementation (Web Audio API synthesis per symbol) coming in Phase 2.
 */

/**
 * Play a synthesised demo sound for the given beatbox symbol.
 * @param {string} symbol  e.g. 'b', 't', 'pf'
 * @param {AudioContext} audioContext
 * @returns {void}
 */
export function playDemo(symbol, audioContext) {
  // Stub — no-op for now
  return
}
